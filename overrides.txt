# sae-vis 0.3.7 declares eindex-callum<0.2, but ARENA installs 0.2.0 from git.
# Without this override the requirements cannot be resolved at all.
eindex-callum @ git+https://github.com/ARENA-education/eindex.git
# CircuitsVis (callum's fork) caps numpy<2, but ARENA's pinned jax 0.10.1 needs numpy>=2.
# The cap is precautionary; the smoke test imports circuitsvis to check it works.
numpy>=2,<3
